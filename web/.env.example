# Base URL prefixed onto every API request. Leave unset/empty for the normal
# same-origin setup (Vite dev proxy locally, nginx in production — see
# vite.config.ts and nginx.conf) — only set this to point the dev server at a
# different origin, which requires CORS support the API server doesn't have.
VITE_API_BASE_URL=
